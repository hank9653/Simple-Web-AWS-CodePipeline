stages:
- name: build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: go-hello-world
      pushRemote: true
      registry: index.docker.io
- name: publish
  steps:
  - applyYamlConfig:
      path: ./deployment.yaml
timeout: 60
